{
  "name": "airrunner-database",
  "version": "1.0.0",
  "description": "Database models and schemas for AirRunner",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongoose": "^8.2.1"
  }
}
